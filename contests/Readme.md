Contest Standings Scripts
=========================

Week 1
------

First update `cfutils` to latest (and uninstall-reinstall).

```
python feed_27_8_23.py
```
Edit contest parameters between lines `# <config>` and `# </config>`.
